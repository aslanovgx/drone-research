sam:
  min_score: 0.8
  min_mask_area: 300
classifier:
  min_confidence: 0.6
outputs:
  json_dir: outputs/dronestock/json
  annotated_dir: outputs/dronestock/predictions
visualization:
  box_thickness: 2
  show_confidence: true
